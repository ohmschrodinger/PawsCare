import 'package:flutter/material.dart';

// ==================== BACKGROUND & CARD COLORS ====================
const Color kBackgroundColor = Color(0xFF121212);
const Color kCardColor = Color(0xFF1E1E1E);

// ==================== TEXT COLORS ====================
const Color kPrimaryTextColor = Colors.white;
const Color kSecondaryTextColor = Color(0xFFB0B0B0);

// ==================== PRIMARY ACCENT & BUTTON COLORS ====================
/// Vibrant teal/aqua for main CTA buttons like "Adopt Now"
const Color kPrimaryAccentColor = Color(0xFF4FC3F7); // Light Sky Blue

/// Legacy amber color - used for navigation bar and quick stats
const Color kLegacyAccentColor = Colors.amber;

// ==================== ACTION BUTTON COLORS ====================
/// "Post" button in community feed
const Color kPostButtonColor = Color(0xFF64B5F6); // Medium Blue

/// "Submit for Review" button for animal listings
const Color kSubmitReviewButtonColor = Color(0xFFFFB74D); // Soft Orange

/// "Submit Application" button for adoption forms
const Color kSubmitApplicationButtonColor = Color(0xFF4CAF50); // Green

// ==================== FILTER BUTTON COLORS (Community Feed) ====================
/// General category filter
const Color kFilterGeneralColor = Color(0xFF81D4FA); // Light Blue

/// Success Story category filter
const Color kFilterSuccessColor = Color(0xFFA5D6A7); // Light Green

/// Concern category filter
const Color kFilterConcernColor = Color(0xFFFFAB91); // Soft Coral

/// Question category filter
const Color kFilterQuestionColor = Color(0xFFBDBDBD); // Light Grey

/// Text color for filter buttons
const Color kFilterTextColor = Color(0xFF424242); // Dark Grey

// ==================== MEDIA BUTTON COLORS ====================
/// Camera button
const Color kCameraButtonColor = Color(0xFF90CAF9); // Light Blue

/// Gallery button
const Color kGalleryButtonColor = Color(0xFFAED581); // Lime Green

/// Icon color for media buttons (dark contrast)
const Color kMediaIconColor = Color(0xFF212121); // Dark Grey

// ==================== ICON COLORS ====================
/// Default icons (subtle and clean)
const Color kDefaultIconColor = Color(0xFFE0E0E0); // Light Grey

/// Interactive/Selected icons
const Color kInteractiveIconColor = Color(0xFF4FC3F7); // Primary accent color

/// White icons for high contrast
const Color kWhiteIconColor = Colors.white;

// ==================== ADDITIONAL THEME COLORS ====================
/// Avatar accent for anonymous/profile fallback
const Color kAvatarAccentColor = Colors.blueAccent;

/// Success green for notifications
const Color kSuccessGreenColor = Color(0xFF6E8C6A); // Muted green
